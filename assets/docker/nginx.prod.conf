# ==========================================
# 1. LEGORS.RU (Основной сайт)
# ==========================================

server {
    listen 80;
    server_name legors.ru www.legors.ru;
    location / { return 301 https://$host$request_uri; }
}

server {
    listen 443 ssl;
    server_name legors.ru www.legors.ru;

    ssl_certificate /etc/letsencrypt/live/legors.ru/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/legors.ru/privkey.pem;

    # --- FIX AUTH ---
    location ^~ /api/auth {
        proxy_pass http://frontend:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /api {
        proxy_pass http://backend:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location / {
        proxy_pass http://frontend:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
    }
}

# ==========================================
# 2. ШАДИПОБЕДА.РФ (Второй сайт)
# ==========================================

server {
    listen 80;
    # ПРАВИЛЬНЫЙ PUNYCODE
    server_name xn--80aaclbft2ci0g.xn--p1ai www.xn--80aaclbft2ci0g.xn--p1ai;
    location / { return 301 https://$host$request_uri; }
}

server {
    listen 443 ssl;
    # ПРАВИЛЬНЫЙ PUNYCODE
    server_name xn--80aaclbft2ci0g.xn--p1ai www.xn--80aaclbft2ci0g.xn--p1ai;

    # ПУТИ К НОВЫМ СЕРТИФИКАТАМ (обрати внимание на папку)
    ssl_certificate /etc/letsencrypt/live/xn--80aaclbft2ci0g.xn--p1ai/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/xn--80aaclbft2ci0g.xn--p1ai/privkey.pem;

    location / {
        root /usr/share/nginx/shadi-pobeda;
        index index.html;
        try_files $uri $uri/ /index.html;
    }
}