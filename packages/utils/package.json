{
  "name": "@tealdeer/utils",
  "version": "0.0.0",
  "private": true,
  "main": "./index.ts",
  "scripts": {
    "lint": "eslint . && prettier --check .",
    "fix": "eslint . --fix && prettier --write ."
  },
  "devDependencies": {
    "@tealdeer/tooling-config": "workspace:*"
  },
  "dependencies": {}
}
